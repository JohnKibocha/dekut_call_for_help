<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DekutCallForHelp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/material_background_dark</item>
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textColor">#FFFFFF</item> <!-- white -->
        <item name="android:fontStyle">normal</item>
        <item name="android:fontWeight">400</item>
        <item name="android:statusBarColor">@color/material_background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>