<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DekutCallForHelp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/material_neutral_light</item>
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textColor">#000000</item> <!-- black -->
        <item name="android:fontStyle">normal</item>
        <item name="android:fontWeight">400</item>
        <item name="android:statusBarColor">@color/material_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.DekutCallForHelp" parent="Base.Theme.DekutCallForHelp" />
</resources>